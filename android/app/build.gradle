plugins {
  id("com.android.application")
  id("org.jetbrains.kotlin.android")
}

android {
  namespace = "org.actikey"
  compileSdk = 34
  defaultConfig {
    minSdk = 26
    targetSdk = 34
    // Exactly two typing languages.
    resourceConfigurations.addAll(listOf("en", "fa"))
    testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
  }
  buildTypes {
    release { isMinifyEnabled = true; isShrinkResources = true }
    debug {}
  }
  flavorDimensions.add("mode")
  productFlavors {
    create("actikey") { dimension = "mode" }
  }
  compileOptions { sourceCompatibility = JavaVersion.VERSION_17; targetCompatibility = JavaVersion.VERSION_17 }
  kotlinOptions { jvmTarget = "17" }
}

dependencies {
  // HeliBoard pinned sources are merged via submodule (vendor/HeliBoard);
  // overlay compiles against them. Zero new heavy deps by policy:
  // no OkHttp/Retrofit/cronet/Gson — JSON via org.json (platform), HTTP via
  // java.net.HttpURLConnection, secure storage via androidx.security-crypto.
  implementation("androidx.security:security-crypto:1.1.0")
  implementation("androidx.preference:preference-ktx:1.2.1")
  testImplementation("junit:junit:4.13.2")
  testImplementation("org.robolectric:robolectric:4.11.1")
}
